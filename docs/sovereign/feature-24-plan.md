Feature #24: Energy Agent / propose_ui UX layout & clarity fixes

Signals: 12 complaints in 14d on propose_ui surfaces + Energy Agent mind metrics.

Issue: No such surfaces, endpoints, or instrumentation exist in repo (public/ is vanilla JS; api/ has none). Underspecified (no data model, hooks, or concrete user flows).

Plan:
- Defer to backlog until owner provides mock + retention metric target.
- If forced: add minimal propose_ui shell in public/ (new tab + metrics stub) behind flag, but violates "minimal correct" rule.
- Next step: request clarification on exact surfaces/metrics before any ship.