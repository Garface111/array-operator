# Eversource Ship Plan (feature #5)

**Status**: BACKLOG (per founder review 2026-07-15).

**Decision**: Do not promote to catalog or build bespoke adapter (`api/adapters/eversource.py`) until ≥1 paying operator in CT/MA/NH exists.

**Next**: Re-evaluate on first Eversource pilot signup. Tracked in Link-utility picker as in-progress (CSV: eversource_ct, eversource, eversource_ma).

**Shipped artifact**: This plan file (research-only, no code change).